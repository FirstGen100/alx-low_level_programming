#include "lists.h"
/**
 * add_dnodeint - add a new node at start of list
 * @head: start of list
 * @n: value to add
 * Return: address of new element
 */
dlistint_t *add_dnodeint(dlistint_t **head, const int n)
{
	dlistint_t *new_node = malloc(sizeof(dlistit_t));
	
new	if (
}
