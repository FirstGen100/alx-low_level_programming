#include "main.h"
#include <stdio.h>
/**
* _puts - prints a string followed by a new line
* @str: a pointer to the string variable
* Return: always 0
*/
void _puts(char *str)
{
printf("%s\n", str);
}
